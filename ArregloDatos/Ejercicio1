#include <stdio.h>

int main() {
    int numeros[5] = {10, 20, 30, 40, 50};

    // se muestran los valores y sus direcciones
    for(int i = 0; i < 5; i++) {
        printf("numeros[%d] = %d \n Dirección: %p\n", i, numeros[i], &numeros[i]);
    }

    return 0;
}